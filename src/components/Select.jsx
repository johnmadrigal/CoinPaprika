import React from 'react';

const Select = (props) => {
  const { coins } = props;
  return (
    <select>
      <option value="Bitcoin">Bitcoin</option>
    </select>
    );
}
 
export default Select;